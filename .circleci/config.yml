version: 2.1

orbs:
  heroku: circleci/heroku@1.0.1
  node: circleci/node@1.1.6

commands:
  add-dist-folder-to-git:
    steps:
      - cp -r build dist
      - git config user.name "github-pipelines"
      - git config user.email "commits-noreply@github.com"
      - force="-f"
      - git add .
      - git commit -m "add dist folder"

jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - yarn install
            - yarn dev:test
            - yarn dev:build
            - persist_to_workspace:
                root: build
                paths:
                  - ./
  deploy:
    executor:
      name: heroku/default
    steps:
      - checkout
      - heroku/install
      - attach_workspace:
          at: ./build
      - add-dist-folder-to-git
      - heroku/deploy-via-git

workflows:
  heroku_deploy:
    jobs:
      - build-and-test
      - deploy:
          requires:
            - build-and-test
          filters:
            branches:
              only:
                - master
                - development
